<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="blog.springboot.mapper.TestMapper">
    <select id="select" resultType="blog.springboot.module.User">
        select * from user
    </select>
</mapper>